import styled from 'styled-components';

export const Wrapper = styled.div`
  /* background-color: lightcoral; // TODO: chwilowo */
`;

export const ImageWrapper = styled.div`
  position: absolute;
  top: 0;
  right: 0;
`;
